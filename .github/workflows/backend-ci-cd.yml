name: CI/CD Backend

on:
  push:
    branches: ["master"]
  workflow_dispatch:

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/task-cards-backend

jobs:
  test-build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Repository auschecken
        uses: actions/checkout@v5

      - name: Java einrichten
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Maven-Tests ausführen
        run: mvn -B test

      - name: Bei GHCR anmelden
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker-Metadaten erzeugen
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha

      - name: Docker-Image bauen und pushen
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

#  deploy:
#    needs: test-build-push
#    runs-on: ubuntu-latest
#    if: github.ref == 'refs/heads/master'
#    environment: production
#
#    steps:
#      - name: Per SSH auf dem Server deployen
#        uses: appleboy/ssh-action@v1.2.0
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            cd /opt/apps/task
#            docker compose --env-file /opt/apps/.env -f compose.server.yaml pull
#            docker compose --env-file /opt/apps/.env -f compose.server.yaml up -d
